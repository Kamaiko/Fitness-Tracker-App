/**
 * List Components - Main Export
 *
 * FlashList-based optimized list components.
 * All components use FlashList internally for efficient recycling.
 *
 * Usage:
 *   import { WorkoutList } from '@/components/lists';
 *
 * Future components (as patterns emerge):
 * - ExerciseList (Phase 3.11.2 - 1,300+ exercises)
 * - ExerciseSelector (Phase 2.7.1 - modal with search)
 * - TemplateList (Phase 5 - workout templates)
 */

export { WorkoutList } from './WorkoutList';
export { WorkoutListItem } from './WorkoutListItem';
